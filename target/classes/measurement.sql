create table measurement
(
    id        integer generated by default as identity
        primary key,
    value     numeric default 0.0   not null,
    raining   boolean default false not null,
    sensor_id integer
        references sensor
            on delete cascade
);

alter table measurement
    owner to postgres;

